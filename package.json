{
  "name": "ready2agent-learn",
  "version": "1.2.2",
  "private": false,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "check:structure": "node scripts/check-structure.mjs",
    "check:prompts": "node scripts/check-prompts.mjs",
    "check:boundary": "node scripts/check-boundary.mjs",
    "check:routes": "node scripts/check-routes.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:content-drift": "node scripts/check-content-drift.mjs",
    "quality": "npm run lint && npm run build && npm run typecheck && npm run check:structure && npm run check:prompts && npm run check:boundary && npm run check:routes && npm run check:links && npm run check:content-drift"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.2.1",
    "next": "^16.2.1",
    "postcss": "^8.5.8",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "typescript": "^5"
  }
}
