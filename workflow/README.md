# Workflow

`workflow/` descrive il processo didattico Ready2Agent Learn.

Flusso standard:

`requisito -> analisi -> architettura -> task -> implementazione -> validazione -> documentazione`

Contenuto attuale:

- `workflow/requirements/r2a-learn-auth-iam-case-study/`

Quality gate:

- `npm run lint`
- `npm run build`
- `npm run typecheck`
